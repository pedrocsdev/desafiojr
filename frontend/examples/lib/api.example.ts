/**
 * Exemplo: cliente HTTP para a API NestJS.
 * Implemente em lib/api.ts
 *
 * Endpoints:
 * - GET    /clients
 * - GET    /clients/:id
 * - POST   /clients
 * - PUT    /clients/:id
 * - DELETE /clients/:id
 */

const API_URL = process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:3000';

// TODO: request helper com tratamento de erro (message do Nest)
// TODO: clientsApi.list(filters)
// TODO: clientsApi.getById(id)
// TODO: clientsApi.create(payload)
// TODO: clientsApi.update(id, payload)
// TODO: clientsApi.remove(id)

export {};
