/**
 * Exemplo: filtros da listagem.
 * Implemente em components/clients/ClientFilters.tsx
 *
 * Campos: busca por nome, busca por email, filtro por status, botão buscar
 */

// TODO: props { filters, onChange, onSearch, loading? }

export {};
