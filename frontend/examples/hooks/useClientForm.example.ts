/**
 * Exemplo: hook do formulário (create + edit).
 * Implemente em hooks/useClientForm.ts
 *
 * Responsabilidades:
 * - valores do form, loading, error, success
 * - loadClient(id) na edição
 * - submit() → create ou update
 * - validateClientForm(): campos obrigatórios, email válido
 */

// TODO: 'use client'
// TODO: validações frontend
// TODO: tratar erros 403/409 da API

export {};
